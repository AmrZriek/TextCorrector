# TextCorrector v3.1 — Python dependencies
# Install with: pip install -r requirements.txt

# GUI framework (Qt 6)
PyQt6>=6.6.0

# Global hotkey capture + Ctrl+C/Ctrl+V injection
keyboard>=0.13.5

# Clipboard access
pyperclip>=1.8.2

# HTTP client (llama.cpp OpenAI-compatible API + streaming)
requests>=2.31.0

# ── Build / release only (uncomment when building) ──────────────────────────
# pyinstaller>=6.0

# ── Dev / tests only ────────────────────────────────────────────────────────
# pytest>=7
# pytest-qt>=4
